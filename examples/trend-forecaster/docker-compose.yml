version: '3.8'

services:
  trend-forecaster:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: trend-forecaster-service
    ports:
      - "3008:3008"
    environment:
      - SERVICE_NAME=trend-forecaster
      - SERVICE_DESCRIPTION=AI-powered Trend forecaster - LLM-based trend prediction using Claude for competitor analysis
      - PORT=3008
      - WALLET_ADDRESS=${WALLET_ADDRESS:-DeDDFd3Fr2fdsC4Wi2Hi7MxbyRHokst3jcQ9L2V1nje3}
      - PRICE_USDC=${PRICE_USDC:-2.00}
      - NETWORK=${NETWORK:-solana-devnet}
      - USDC_MINT=${USDC_MINT:-4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU}
      - SOLANA_RPC_URL=${SOLANA_RPC_URL:-https://api.devnet.solana.com}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - NODE_ENV=${NODE_ENV:-development}
      - RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS:-900000}
      - RATE_LIMIT_MAX_REQUESTS=${RATE_LIMIT_MAX_REQUESTS:-100}
      - MAX_SCRAPE_TIMEOUT_MS=${MAX_SCRAPE_TIMEOUT_MS:-30000}
      - USER_AGENT=${USER_AGENT:-AgentMarket-WebScraper/1.0}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3008/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - agentmarket-network

networks:
  agentmarket-network:
    name: agentmarket-network
    external: true
