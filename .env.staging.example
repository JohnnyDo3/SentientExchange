# Staging Environment Variables
# Configure these in Railway Dashboard for staging environment

# CDP Wallet Configuration (REQUIRED)
# Use testnet credentials for staging
CDP_API_KEY_NAME=your-staging-cdp-api-key-name
CDP_API_KEY_PRIVATE_KEY=your-staging-cdp-private-key

# Network (use testnet for staging)
NETWORK=base-sepolia

# Database (Railway will auto-inject DATABASE_URL if using PostgreSQL addon)
# For SQLite on Railway:
DATABASE_PATH=/app/data/agentmarket.db
# For PostgreSQL on Railway (recommended):
# DATABASE_URL is automatically injected by Railway

# Server Configuration
NODE_ENV=staging
PORT=$PORT  # Railway injects this automatically

# API Configuration
API_PORT=8081

# Logging
LOG_LEVEL=info

# Feature Flags
ENABLE_ANALYTICS=true
ENABLE_X402_PAYMENTS=true

# Railway Configuration (injected automatically by Railway)
RAILWAY_ENVIRONMENT=staging
RAILWAY_SERVICE_NAME=agentmarket-mcp

# Optional: External Service URLs
# Configure if using external services in staging
# EXTERNAL_SERVICE_URL=https://staging-api.example.com
