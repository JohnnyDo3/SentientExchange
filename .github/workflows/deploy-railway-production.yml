name: Deploy to Production (Railway)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy-production:
    name: Deploy to Railway Production
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    container: ghcr.io/railwayapp/cli:latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Railway Production
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_SERVICE_ID: ${{ secrets.RAILWAY_SERVICE_ID }}
        run: |
          railway up --service=${{ env.RAILWAY_SERVICE_ID }} --environment=production

      - name: Create deployment tag
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          TAG="v$(date +%Y%m%d-%H%M%S)"
          git tag -a "$TAG" -m "Production deployment $TAG"
          git push origin "$TAG"

      - name: Notify on success
        if: success()
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_SERVICE_ID: ${{ secrets.RAILWAY_SERVICE_ID }}
        run: |
          echo "✅ Production deployment succeeded"
          echo "Environment: main branch → Railway production"
          URL=$(railway domain --environment production --service=${{ env.RAILWAY_SERVICE_ID }})
          echo "Production URL: https://$URL"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Production deployment failed"
          exit 1
