# Production Environment Variables
# Configure these in Railway Dashboard for production environment

# CDP Wallet Configuration (REQUIRED)
# Use mainnet credentials for production
CDP_API_KEY_NAME=your-production-cdp-api-key-name
CDP_API_KEY_PRIVATE_KEY=your-production-cdp-private-key

# Network (use mainnet for production)
NETWORK=base

# Database (Railway will auto-inject DATABASE_URL if using PostgreSQL addon)
# For PostgreSQL on Railway (strongly recommended for production):
# DATABASE_URL is automatically injected by Railway
# For SQLite (not recommended for production):
# DATABASE_PATH=/app/data/agentmarket.db

# Server Configuration
NODE_ENV=production
PORT=$PORT  # Railway injects this automatically

# API Configuration
API_PORT=8081

# Logging
LOG_LEVEL=warn

# Feature Flags
ENABLE_ANALYTICS=true
ENABLE_X402_PAYMENTS=true

# Railway Configuration (injected automatically by Railway)
RAILWAY_ENVIRONMENT=production
RAILWAY_SERVICE_NAME=agentmarket-mcp

# Security Configuration
# Add these for production security
CORS_ORIGIN=https://yourdomain.com
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# Monitoring (optional)
# SENTRY_DSN=your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-newrelic-key

# Optional: External Service URLs
# Configure if using external services in production
# EXTERNAL_SERVICE_URL=https://api.example.com
